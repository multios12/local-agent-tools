{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Debug MCP Server",
			"type": "go",
			"request": "launch",
			"mode": "debug",
			"program": "${workspaceFolder}/cmd/mcpserver",
			"cwd": "${workspaceFolder}",
			"envFile": "${workspaceFolder}/.env.debug",
			"console": "integratedTerminal"
		}
	]
}
